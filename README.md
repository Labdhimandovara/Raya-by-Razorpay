# ⚡ Raya by Razorpay — Autonomous Multilingual AI Shopping Agent & Bazaar AI Merchant Growth Engine

**Raya by Razorpay** is a closed-loop agentic commerce system connecting buyers to live multi-store catalogs and merchant growth intelligence with native **5-Language Multilingual Support** across India's top linguistic regions:

1. **English (`en`)**
2. **हिन्दी — Hindi (`hi`)**
3. **मराठी — Marathi (`mr`)**
4. **தமிழ் — Tamil (`ta`)**
5. **বাংলা — Bengali (`bn`)**

Raya is not merely a conversational chat interface or a checkout widget. It is an end-to-end, policy-gated autonomous commerce engine:

```
Customer Intent ➔ AI Multi-Store Discovery ➔ Smart Recommendations ➔ Merchant Growth Strategy
        ▲                                                                           │
        │                                                                           ▼
Learning / Feedback ◄─ Revenue Attribution ◄─ Settlement ◄─ Razorpay Payment ◄─ 6 Gates Check
```

---

## 🌐 5-Language Multilingual Architecture

Raya and Bazaar feature complete, zero-leakage multilingual coverage across all components with strict key parity and persistent locale state.

### Supported Languages
| Code | Language | Native Script | Target Demographics |
| :--- | :--- | :--- | :--- |
| `en` | English | English | Global & Pan-India Tech Shoppers |
| `hi` | Hindi | हिन्दी | North & Central India |
| `mr` | Marathi | मराठी | Western India (Maharashtra) |
| `ta` | Tamil | தமிழ் | Southern India & Global Tamil diaspora |
| `bn` | Bengali | বাংলা | Eastern India (West Bengal) & Bangladesh |

### Multilingual Coverage Matrix
When a language is selected, the **entire application** dynamically adapts with zero language mixing:

* **Buyer AI Chat (`/`)**:
  * Agent conversational tone & recommendations natively generated by Google Gemini in the target language.
  * Search keywords automatically canonicalized from regional vocabulary (e.g., `लैपटॉप` ➔ `laptop`, `ஹெட்ஃபோன்` ➔ `headphone`) to query multi-store bridge catalogs.
  * Voice Search Speech Recognition (`webkitSpeechRecognition` / `SpeechRecognition`) tuned to selected locale (`hi-IN`, `mr-IN`, `ta-IN`, `bn-IN`, `en-IN`).
* **Interactive Product Cards**:
  * Match reason labels, compatibility explanations, confidence scores, and action buttons.
* **Cart Drawer & Governance**:
  * Item counts, summary subtotals, companion cross-sell suggestions, and 6-gate safety checklist.
* **Razorpay Payment Integration**:
  * Payment authorization buttons, synthetic test-mode settlement notices, and localized status notifications.
* **Order Confirmation Receipt**:
  * Shipping destination, fulfillment partner store pill, payment verification badge, and order ID tracking.
* **Bazaar Merchant Growth Control Room (`/merchant`)**:
  * Verified Business Impact cards (Total GMV, AI-Attributed GMV, Incremental GMV, Conversion Rate, AOV).
  * 10-Step Closed-Loop Commerce Story Pipeline visualization.
  * Before vs After Bazaar AI comparison matrix.
  * 6 Server-Side Financial Guardrails and Interactive Failure Demos.
  * Live Orders Telemetry and Decision Ledger audit trail.
* **Merchant Copilot (`/api/merchant/copilot`)**:
  * Copilot assistant answers queries regarding live sales, attach rates, and growth opportunities completely in the merchant's chosen language.

### Localization Persistence & Key Parity
* **Persistent Key**: Stored in `localStorage` under `raya_bazaar_locale`.
* **Seamless Transitions**: Survives page refreshes, navigation between buyer chat (`/`) and merchant console (`/merchant`).
* **100% Dictionary Key Parity**: Guaranteed across `en.json`, `hi.json`, `mr.json`, `ta.json`, and `bn.json` (verified by automated CI test suites).

### Brand Name & Enum Immutability Rules
To prevent business logic corruption or confusing translations:
* **Protected Brands (Original Latin Form)**: `Raya`, `Bazaar AI`, `Razorpay`, `NexusStore`, `ThreadVault`, `PixelMart`, `eBay`.
* **Protected Enums (Canonical Tokens)**: `DISCOVERED`, `RECOMMENDED`, `ADDED`, `POLICY_CHECKED`, `APPROVED`, `PAYMENT_CREATED`, `PAID`, `ATTRIBUTED`, `BLOCKED`, `INVALIDATED`.
* **Product Titles, Strategy IDs & Order IDs**: Maintained in exact numeric/identifier form.

---

## 🏪 Multi-Store Bridge Pipeline

Raya connects simultaneously to 3 specialized live merchant stores plus the global eBay marketplace through the Bazaar Bridge Gateway:

1. **⚡ NexusStore (`nexusstore`)**:
   * *Vibe*: High-performance smart apparel, connected wearable jackets, and daily tech electronics.
2. **🧵 ThreadVault (`threadvault`)**:
   * *Vibe*: Minimalist luxury fashion, Mongolian cashmere, bespoke heavyweight streetwear, and audiophile acoustic gear.
3. **🎮 PixelMart (`pixelmart`)**:
   * *Vibe*: Cyberpunk creator gear, streaming equipment, macro keypads, and RGB desktop hardware.
4. **🛍️ eBay Marketplace (`ebay`)**:
   * *Vibe*: Worldwide marketplace with certified refurbished flagship electronics and direct marketplace listings.

---

## 🛡️ The 6 Server-Side Financial Guardrails

Raya enforces strict, server-side financial and safety bounds before any transaction touches Razorpay:

1. **Gate 01: Pre-Purchase Policy Limits**: Maximum spend cap per transaction (configurable by merchant).
2. **Gate 02: Price Volatility Guard**: Verifies that catalog price has not spiked between recommendation and payment.
3. **Gate 03: Quantity & Rate Bounds**: Restricts excessive unit purchases to curb bot scalping.
4. **Gate 04: Currency Verification**: Domestic INR enforcement to prevent unauthorized forex slippage.
5. **Gate 05: Merchant Authorization**: Fulfillment only via active, verified Razorpay merchant accounts.
6. **Gate 06: Approval Expiry TTL**: User approval tokens expire after 15 minutes to prevent replay transactions.

---

## 🧪 Automated Verification Test Suites

Two automated verification suites are included in `tests/` to guarantee code and localization integrity:

### 1. Multilingual Verification Suite
Verifies all 5 locale dictionaries, 100% key parity, `LocaleProvider`, API endpoints, and brand/enum preservation:
```bash
node tests/multilingual-verification.test.js
```
*Expected output: `ALL MULTILINGUAL VERIFICATION TESTS PASSED (6/6)!`*

### 2. Track 01 Commerce Verification Suite
Verifies active strategy injection, conservative attribution, UI tool sanitization, and failure demo triggers:
```bash
node tests/track01-verification.test.js
```
*Expected output: `ALL AUTOMATED VERIFICATION CHECKS PASSED (5/5)!`*

---

## ⚙️ Environment Variables

Configure the following in `.env.local` or your Vercel Dashboard:

| Variable | Description |
| :--- | :--- |
| `GEMINI_API_KEY` | Google Gemini API Key for autonomous reasoning and multilingual generation |
| `BAZAAR_BRIDGE_URL` | Live Multi-Store Bridge Gateway URL |
| `NEXT_PUBLIC_APP_NAME` | Application title (`Raya by Razorpay`) |
| `NEXT_PUBLIC_RAZORPAY_KEY_ID` | Razorpay Test Key ID (`rzp_test_...`) |

---

## 🚀 Getting Started

### 1. Install Dependencies
```bash
npm install
```

### 2. Run Development Server
```bash
npm run dev
```
Open [http://localhost:3000](http://localhost:3000) for the Raya Buyer UI, or [http://localhost:3000/merchant](http://localhost:3000/merchant) for the Bazaar AI Merchant Control Room.

### 3. Production Build
```bash
npm run build
npm start
```
